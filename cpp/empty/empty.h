#ifndef EMPTY_H
#define EMPTY_H

class Empty
{
private:

public:
    // Empty(); // 컴파일러가 자동으로 주석처리된 선언들을 만들어 준다.
    // Empty(Const Empty& rhs);
    // ~Empty();
    // Empty& operator=(const Empty& rhs);
    // Empty& operator&();
    // const Empty& operator&() const 
};

#endif